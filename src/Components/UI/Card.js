import styled from "styled-components";

const CardWrapper = styled.div`
  width: 60rem;
  height: 15rem;
  background: #bbb;
  border-radius: 15px;
`;

const Card = () => {
  return <CardWrapper></CardWrapper>;
};

export default Card;
